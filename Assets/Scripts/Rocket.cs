using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Rocket : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    private void Awake()
    {
        //rocket goes to next person ahead.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
